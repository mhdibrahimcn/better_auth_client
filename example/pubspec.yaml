name: better_auth_client_example
description: Demonstrates how to use the better_auth_client package.
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  better_auth_client:
    path: ../

flutter:
  uses-material-design: true
